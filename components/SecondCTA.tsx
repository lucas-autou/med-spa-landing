'use client';

export default function SecondCTA() {
  return (
    <section className="w-full py-8 text-center">
      <button className="bg-teal-600 text-white px-8 py-4 rounded-lg font-semibold text-lg hover:bg-teal-700 transition-colors shadow-lg hover:shadow-xl">
        Start Your 14-Day Pilot
      </button>
      <p className="text-sm text-gray-600 mt-3">
        $297 one-time — fully credited if you upgrade.
      </p>
    </section>
  );
}